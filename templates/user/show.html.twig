{% extends 'base_admin.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    
<div class="card">
  <div class="card-header">
                <table class="table">
        <tbody>
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ user.Adress }}</td>
            </tr>
            <tr>
                <th>Phone_number</th>
                <td>{{ user.phoneNumber }}</td>
            </tr>
            <tr>
                <th>Education</th>
                <td>{{ user.education }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ user.description }}</td>
            </tr>
            <tr>
                <th>Gender</th>
                <td>{{ user.gender }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
        </tbody>
    </table>

  </div>
  <div class="card-body">
    

  </div>
</div>



   <button class='btn btn-primary' > <a href="{{ path('user_index') }}" style='color:white' > back to list</a></button> 

    <button class='btn btn-warning' > <a href="{{ path('user_edit', {'id': user.id}) }}" style='color:white'>edit</a></button>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
